# VOLATILE 게임 구조

> 최신 버전: **v0.7** (`VOLATILE_v0.7.html`). v0.5까지의 4속성 반응 시스템은 "너무 복잡하고 직관적이지 않다"는 판단으로 폐기했고, 이 문서는 v0.6 규칙만 다룬다. 이전 규칙은 `VOLATILE_v0.5.html`과 `작업내용.md` 이력에 남아 있다.

## 1. 한 줄 요약

정통 판타지 던전을 내려가는 **연금술사 모험가**의 인벤토리 로그라이크. 규칙은 두 줄이다.
- **옆에 두면 강해진다.** 패시브 아이템은 상하좌우 옆 칸의 아이템을 강화한다.
- **클릭하면 쓴다.** 아이템마다 에너지가 들고, 턴마다 에너지 3이 찬다.

반응·속성·타이머·상성표는 없다. 아이템은 그림 + 숫자 하나 + 한 줄 설명으로 전부 설명된다.

## 2. 파일 구조 (단일 HTML)

| 구획 | 내용 |
| --- | --- |
| `<style>` | 다크 테마, 가방 그리드(`--cs` 셀 크기), 던전 장면(`--sh` 높이), 하단 시트, 시너지 강조 |
| HTML | `#title` · `#run`(상단바 → 힌트 → 가방 → 던전 장면 → 액션바) · `#sheet-root`(보상·이벤트·캠프·결과) |
| `SPRITES` | 픽셀 아트 문자열 → SVG. 아이템·적·주인공·장면 타일·오브젝트 |
| `ITEMS` / `POOL` / `PASSIVES` | 아이템 16종 정의와 층별 보상 풀 |
| `ENEMIES` / `ENCOUNTERS` | 적 6종 + 보스, 층별 조우표 |
| 가방 함수 | `mkBag`, `canPlace`, `addItem`, `neighbors`, **`eff`**(옆 시너지를 합친 실제 수치) |
| `Battle` | 에너지·방어·아이템 사용·적 행동 |
| `UI` | 가방 렌더(숫자·에너지 점·시너지 강조·고스트 미리보기), 장면 렌더, 애니메이션 |
| `Screens` | 탐험 → 조우 → 보상/이벤트/캠프 시트 → 다음 층 |

## 3. 아이템

```
{ name, spr(스프라이트 키), w, h, type, cost, dmg|block|heal, tags[], desc,
  aura:{tag, dmg|block|heal}   // 패시브: 옆 칸 중 tag를 가진 아이템 강화
  startBlock, energy, consume, aoe, pierce, burn, poison, freeze }
```

| 유형 | 아이템 | 수치 | 비고 |
| --- | --- | --- | --- |
| 공격 | 단검 2 · 화염 플라스크 3(화상 2) · 냉기 플라스크 2(1턴 동결) · 산성 플라스크 3(방어 무시) · 독 플라스크 1(중독 2×3턴) · 폭탄 4(전체, 에너지 2) | 에너지 1 | 꼬리표: 무기 / 플라스크 / 불 |
| 방어 | 방패 4 | 에너지 1 | |
| 회복 | 치유 물약 5 | 에너지 1, 1회용 | |
| 패시브 | 화로(옆 불 +2) · 촉매(옆 플라스크 +1) · 숫돌(옆 무기 +2) · 부적(옆 방패 +2) · 약초(옆 물약 +3) · 가죽 갑옷(턴 시작 방어 2) · 마력 결정(에너지 +1) · 불사조 깃털(옆 불 +3, 보스 드롭) | 에너지 0 | 놓기만 해도 효과 |

시작 가방: 화로·화염 플라스크·단검·방패·치유 물약 (5×4). 화로 옆에 화염 플라스크를 두면 5 피해가 되는 것이 첫 튜토리얼.

## 4. 전투

```
턴 시작 : 에너지 = 3 + 마력 결정, 방어 = 가죽 갑옷 합계, 아이템 사용 표시 초기화
플레이어: 아이템 클릭 → (공격이면 적 클릭) → 에너지 차감, 이번 턴 사용 완료(흐려짐)
          옮기기·회전은 에너지 1 (탐험 중에는 무료)
턴 종료 : 적마다 화상·중독 피해 → 동결이면 스킵 → 공격 (방어가 먼저 흡수)
```
- 적: 동굴 쥐(6/2), 화염 박쥐(5/3, 냉기 약점), 젖은 슬라임(10/2), 서리 유령(8/2, 불 약점), 고블린(7/1×2), 갑옷 딱정벌레(12/4, 방어 3), 보스 용광로 지킴이(40/3, 3턴마다 숨결 8).
- 약점은 2배, 방어는 피해에서 차감(산성은 무시), 화상은 매 턴 1씩 줄며 그만큼 피해, 중독은 3턴.

## 5. 런

- 층당 6단계: 전투 → (전투/정예/보물/이벤트 2지선다 ×3) → 정예 → 캠프. 3층 캠프 뒤 최하층 보스.
- 전투 보상: 층별 풀에서 3중 1. 정예·보물: 패시브 포함 3중 1. 자리가 없으면 위 가방에서 버리고 넣거나 넘어간다.
- 이벤트 3종: 치유의 샘(HP 10) · 낡은 배낭(가방 +1행) · 수상한 상인(물약 2개, HP -5).
- 캠프: 휴식(HP 15) / 가방 확장(+1행) / 단련(최대 HP +5).

## 6. 화면 구도 (백팩 히어로식)

위는 가방, 아래는 던전 장면. 탐험 중에는 장면 오른쪽에 갈 곳 1~2개가 오브젝트(적 실루엣·보물 자루·팻말·모닥불·문)로 서고, 클릭하면 연금술사가 걸어가 조우한다. 전투는 같은 장면에서 적이 오른쪽에 서며, 아이템을 고르고 적을 클릭하면 아이템이 날아간다. 보상·이벤트·캠프·결과는 가방을 가리지 않는 하단 시트.

## 7. 가독성 장치

- 아이템 칸: 스프라이트, 이름, 에너지 점(●), 큰 숫자(공격 빨강·방어 파랑·회복 초록·패시브 금색 +N). 시너지로 올라간 숫자는 금색.
- **설명 카드**: 아이템을 탭/클릭하면 가방 바로 아래에 그림·이름·유형·에너지·현재 수치·한 줄 설명·시너지 출처("옆 화로 덕분에 +2")가 뜬다. 전투 중에는 카드에 '사용' 버튼이 있고, 같은 아이템을 한 번 더 탭해도 쓴다. 공격은 적을 탭. 터치 기기에 툴팁이 없는 문제를 이것으로 해결.
- 아이템을 선택하면 그것을 강화하는 패시브(주황 테두리)와 그것이 강화하는 아이템(금색 안쪽 테두리)이 표시된다.
- 옮길 자리에 마우스를 올리면 고스트와 함께 "여기 두면 N" 힌트가 뜬다.
- 힌트 줄 하나가 튜토리얼을 겸한다(첫 탐험, 첫 전투). 규칙 창은 5줄.
- 정보 패널·반응 로그는 없다. 액션바 왼쪽에 최근 3줄 요약만.

## 8. 반응형

가방 셀 `--cs = clamp(44px, min(11vw, 9.2vh), 104px)`, 장면 높이 `--sh = clamp(200px, 32vh, 380px)`. 무대 최대 폭 1200px 중앙 정렬. 600px 이하에서는 아이템 이름을 숨기고 스프라이트를 키운다. 터치는 첫 탭 고스트, 두 번째 탭 확정.
