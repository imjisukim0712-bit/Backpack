# VOLATILE 게임 구조

> 최신 버전: **v0.2** (`VOLATILE_v0.2.html`) · 이 문서는 프로토타입의 실제 구현 구조를 기준으로 유지합니다.
> 기획 원본은 「VOLATILE 세로 슬라이스 기획서 v0.1」(2026-09-04).

## 1. 한 줄 요약

플레이어(견습 연금술사)는 공격 능력이 없다. 매 턴 가방(5×4 격자) 안의 아이템을 **옮기고, 덮고, 던지는** 것으로 싸운다.
아이템은 **열·냉·습·압** 네 속성에 노출되면 상태가 바뀌고, 상태에 따라 던졌을 때 효과가 달라진다. 적은 HP를 깎는 동시에 가방 환경(속성)을 바꾼다.

## 2. 파일 구조 (단일 HTML 내부 구획)

`VOLATILE_vX.Y.html` 하나에 아래 순서로 들어 있습니다. 외부 의존성 없음.

| 구획 | 내용 |
| --- | --- |
| `<style>` | 다크 테마 토큰, 전투 화면 그리드 레이아웃(데스크톱 2열 / 900px 이하 1열), 셀 크기 `--cs: clamp(44px, 11vw, 72px)` |
| HTML 뼈대 | `#title` 타이틀 · `#map` 노드맵 · `#battle` 전투 · `#camp` 캠프 · `#generic` 채집/이벤트/결과 · 모달·토스트 |
| `DATA` | `ITEMS`(아이템 정의) · `ENEMIES` · `ENCOUNTERS`(층별 조우표) · `RECIPES`(조합 6종) · `EVENTS`(이벤트 5종) |
| `SPRITES` | `PALETTE`(문자→색 28종) · `ITEM_SPRITES`(아이템 상태별 16×16, 32종) · `ENEMY_SPRITES`(적 24×24, 보스 32×32) · `PLAYER_SPRITE`(24×24) · `spriteSVG` 렌더러 |
| `BagState` | `mkBag`, `mkItem`, `itemCells`, `canPlace`, `addItem`, `neighbors`, `occMap` |
| `Solver` | **순수 함수** `Solver.tick(bag, env)` → `{bag, events, playerDmg, scorch, soak, holes, vanished, changed}` |
| `Codex` | 도감. 처음 목격한 전이·레시피를 `localStorage`에 기록 |
| `Run` | 런 상태: HP, 층, 가방, 맵, 통계 |
| `Battle` | 턴 상태 기계, 플레이어 행동, 던지기 판정, 적 행동 |
| `UI` | 전투 화면 렌더(적 카드, 가방 그리드, 남은 틱, 고스트 미리보기, 예측 오버레이, 정보 패널, 로그) |
| `Screens` | 맵·전투 결과(드롭/채집)·캠프·이벤트·도감·도움말·결과 화면 |
| `G` | 버튼/단축키가 호출하는 컨트롤러 |

## 3. 데이터 모델

### 아이템 (`ITEMS[id]`)
```
{ name, w, h, kind, glyph,
  cap(용기 용량), lead(납 상자), blocker(칸막이), cool(도구 쿨타임), heatMul/heatAdd(촉매),
  states: [ { id, name,
              emit:{heat,cold,wet,press},   // 발산 (소금은 wet:-1 = 흡수)
              life, lifeTo,                 // n틱 뒤 소멸 또는 다른 상태로
              tr:[{p, t, n}],               // 전이: 속성 p 카운터가 t 이상이면 상태 n으로
              enter:['explode33'|'explodeCross'|'soak'|'burst'|'explode33s'],
              vanish, material, acid,
              throw:{kind, n, t, dmgType, desc, brink}, use:{kind, n, desc} } ] }
```
`states[0]`이 초기 상태. `brink`는 "폭발 직전(남은 틱 1)"일 때 대신 쓰는 던지기 효과(기름병 광역 6).

### 아이템 인스턴스
`{ uid, id, x, y, rot, st, c:{heat,cold,wet,press}, life, cool, contents:[], boost }`
용기 안의 아이템은 `bag.items`가 아니라 용기의 `contents`에 들어간다.

### 가방
`{ cols, rows, cells:[{hole, sc, soak, acid}], items:[], blast:[{cells, heat}] }`
칸 상태는 겹칠 수 있다. `hole` 구멍(배치 불가) · `sc` 그을림(영구, 캠프 수선) · `soak` 젖음 남은 턴 · `acid` 산성 누적(3이면 구멍).
`blast`는 이번 틱 폭발이 만든 열이며 **다음 틱** 노출에만 반영된다(연쇄 지연 규칙).

### 적 (`ENEMIES[id]`)
`{ name, glyph, hp, atk, floor, env:{heat|cold|wet}, armor, special:'rat'|'slime'|'goblin', fire, slime, wraith, boss, drop, dropN, desc }`

## 4. 반응 시스템 (Solver)

### 노출 계산 `exposure(bag, item, env)`
1. 기본값 = 가방 환경(적·화로가 부여). 용기 안은 예외.
2. 상하좌우 인접한 아이템(여러 칸 아이템도 **한 번만**)의 `emit`을 더한다. 대각선 없음. 칸막이는 아이템이므로 그 너머는 인접이 아니다.
3. 촉매: 인접 불사조 깃털이면 열 ×2, 잉걸 촉매면 열 +1. 풀무 사용 시 해당 발산원 열 +1.
4. 지난 틱 폭발(`blast`) 범위 안이면 그 열을 더한다.
5. 칸 상태: 그을린 칸 열 +1, 젖은 칸이면 열 = 0.
6. 열과 냉은 서로 빼서 순값만 남긴다. 습은 0 이상으로 클램프(소금 흡수).
7. 유리병: 외부 노출 없음, 내용물의 압 발산 합만 `press`로 받는다. 내용물은 노출 0이지만 수명(life)은 흐른다(증기는 1틱 뒤 소멸하며 압 1을 남기고, 산성 증기는 3틱 동안 압 3을 쌓아 병을 터뜨린다). 납 상자: 모든 노출 0, 카운터·수명 정지.

### 틱 `Solver.tick(bag, env)`
1. 모든 아이템의 노출을 **현재 가방** 기준으로 먼저 계산한다.
2. 카운터에 노출을 더하고, 현재 상태의 전이 중 첫 번째로 임계치에 닿은 것으로 전이한다. 카운터는 0으로. 전이가 없고 `life`가 있으면 1 감소, 0이면 소멸(또는 `lifeTo`).
3. 진입 효과(폭발·파열·젖음)는 전이가 전부 끝난 뒤 일괄 적용. 폭발 열은 `blast`에 기록되어 **다음 틱부터** 센다.
4. 소멸 정리 → 젖음 타이머 감소(이번 틱에 새로 젖은 칸 제외) → 산성 증기 인접 칸 누적(3틱이면 구멍, 그 칸 아이템 파괴).

솔버는 도감·저장소·DOM을 건드리지 않는다. 처음 본 전이는 `r.codex`로 돌려주고, 턴을 실제로 커밋하는 `Battle.applyTickResult`에서만 도감에 기록한다(예측 호버는 기록하지 않는다).

같은 함수를 **예측 오버레이**(턴 종료 버튼 호버)와 실제 턴 처리가 함께 쓴다.

### 진입 효과
| 효과 | 내용 |
| --- | --- |
| `explode33` (기름병) | 3×3 그을림, 다음 틱 열 6, 플레이어 3 피해, 아이템 소멸 |
| `explodeCross` (화약) | 십자 칸 구멍(아이템 파괴), 플레이어 5 피해 |
| `soak` (증기) | 인접 칸 2턴 젖음 |
| `burst` (유리병) | 플레이어 2 피해, 내용물 소실, 내용물의 던지기 효과가 적 전체에 적용(`r.aoe`로 반환, Battle이 적용) |
| `explode33s` (불안정 결정) | 다음 틱 3×3 열 3, 플레이어 1 피해 |

## 5. 전투 턴 흐름 (Battle)

```
newTurn  : 행동 2 충전, 환경 재계산(적 env 합산 + 보스 누적 열 + 화로), 쿨타임 감소, 보스 숨결 예고(3턴마다)
PLAYER   : 옮기기(1) · 회전(1) · 자리 교환(2) · 던지기(1) · 덮기/꺼내기(1) · 도구/붕대 사용(1) · 대기(남은 행동 ×2 = 이번 턴 받는 피해 총량 감소)
endTurn  : Solver.tick → 로그 → 가방 안 폭발 피해 적용
enemyPhase: 적마다 [화상/중독 지속 피해] → [동결이면 스킵] → 공격(대기 보너스 풀에서 차감) → 특수(쥐 갉기 3턴, 고블린 훔치기 2턴, 보스 P2 그을림)
환경 재계산: 턴 시작뿐 아니라 적 처치·동결 직후에도 즉시 (`computeEnv`)
checkEnd : HP 0 → 게임오버 / 적 전멸 → 승리(드롭 + 채집 선택)
```

### 던지기 판정 `applyThrow / dealDamage`
- 효과 종류: `dmg`, `aoe`(광역), `burn`(화상 n×t, 즉발 피해 없음), `freeze`(동결), `water`(화상 해제, 불 속성에만 피해), `oiled`(다음 열 피해 ×2), `poison`, `acid`(방어 제거 + 피해), `plant`(대상 적에게 화약 설치: 그 적이 다음에 열 피해를 받으면 +5, 다른 적 전체 5), `jar`(내용물 효과 + 파편 2).
- 화상·중독 지속 피해도 `dealDamage`를 거쳐 약점·기름·방어 규칙을 따른다.
- 피해 타입 `heat/explode/cold/water/acid/phys/poison`. 화염 박쥐는 물·냉 ×2, 서리 유령은 열 ×2 그 외 ½, 딱정벌레 방어 3은 산성·폭발로만 제거.
- 보스 숨결 예고 턴에 물·얼음을 던지면 취소.

## 6. 런 구조 (Screens)

- **층**: 6단계 = 전투 → (2지선다 ×3: 전투 55% / 정예 10% / 채집 15% / 이벤트 15%) → 정예 → 캠프. 3층 캠프 다음이 보스.
- **전투 보상**: 적 드롭 + 채집(반응물 3중 1). 정예는 비소모 아이템(용기·도구) 2중 1.
- **채집 노드**: 반응물 2개 또는 도구 1개.
- **이벤트 5종**: 버려진 화로 / 얼어붙은 샘 / 재봉사의 유령(가방 +1행, 런당 1회) / 수상한 상인(잉걸 촉매 + 구멍 2) / 도둑의 은신처(유리병 2 + 다음 전투 고블린 ×2).
- **캠프**: 수선(그을림 재료 1, 구멍 재료 2) · 조합(재료 2 → 반응물 1, 6종) · 버리기. 재료 = 박쥐 날개·젤리·서리 결정·마른 버섯.
- **도감**: 처음 본 상태 전이와 레시피를 `localStorage`에 기록, 타이틀/맵에서 열람.
- **통계**: 턴 수, 옮기기 비율, 던지기, 대기, 예측 사용 횟수, 가방 점유율(가설 H1~H4 판정용).

## 7. 가독성 규칙 구현 현황

| 규칙 | 구현 |
| --- | --- |
| 남은 틱 상시 표시 | 아이템 우하단 숫자 + 속성 색 (`ticksLeft`) |
| 놓기 전 미리보기 | 선택 후 칸 호버 시 고스트 + 노출·남은 틱. 터치 기기는 첫 탭 고스트, 같은 칸 두 번째 탭 확정 |
| 턴 종료 예측 오버레이 | 버튼 호버 또는 "예측 보기" 토글로 `Solver.tick` 결과를 점선·테두리로 표시. 힌트 줄에 변할 아이템 수·피해·칸 변화 요약 |
| 환경 게이지 | 상단 열·냉·습 값, 적 카드에 환경 태그 |
| 반경 하이라이트 | 폭발·파열 가능 아이템 선택 시 반경 칸 강조 |
| 반응 로그 | 우측 패널, 속성 색 테두리 |
| 4색 고정 | 열 주황 · 냉 파랑 · 습 청록 · 압 보라 |

## 8. 스프라이트 시스템

- 픽셀 아트를 **문자열 배열**로 저장한다. 한 문자열이 한 줄, 한 문자가 한 픽셀. `.`은 투명, 나머지 문자는 `PALETTE`의 색.
- `spriteSVG(rows)`가 가로 연속 픽셀을 `<rect>` 하나로 합쳐 SVG로 만들고 캐시한다(`shape-rendering: crispEdges`).
- 아이템 스프라이트 키는 `아이템id:상태id`(예: `oil:lit`). 상태가 바뀌면 그림도 바뀐다. 키가 없는 일시 상태(폭발·파열·소모)는 같은 아이템의 첫 스프라이트로 대체.
- 칸막이는 세로로 늘려 그리고(`preserveAspectRatio="none"`), 납 상자는 2×2 영역에 크게 그린다.
- 쓰이는 곳: 가방 칸, 고스트 미리보기, 용기 내용물 표시, 정보 패널, 적 카드, HP 바 옆 주인공, 미니 가방(캠프·보상·이벤트), 드롭·보상 목록, 타이틀.
- 새 스프라이트 추가 규격은 팔레트 문자만 사용하고 정사각 크기를 정확히 지킬 것. 외곽선 `k`, 광원 왼쪽 위.

## 9. 반응형

- 900px 이하: 우측 패널(정보·로그)이 가방 아래로 내려가는 1열 레이아웃.
- 셀 크기는 뷰포트에 비례(`clamp`), 5열 가방이 390px 폰에서도 화면 안에 들어간다.
- 모든 조작은 클릭/탭(드래그 불필요). 단축키 R/E/Esc는 보조.
- 아이템 유형은 테두리 스타일(발산원 이중선, 용기 점선, 도구 점선, 촉매 금색)로 구분한다. 열·냉·습·압 4색은 속성 이외의 뜻으로 쓰지 않는다.
- 하단 액션바와 토스트는 `env(safe-area-inset-bottom)`을 반영한다.
