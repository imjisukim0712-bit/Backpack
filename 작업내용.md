# 작업내용

버전별 작업 이력. 최신이 위.

## v0.1 — 2026-09-04 · 첫 프로토타입

**산출물**: `VOLATILE_v0.1.html` (단일 파일, 외부 의존성 없음), `README.md`, `게임구조.md`, `작업내용.md`

**구현 범위** (기획서 v0.1 세로 슬라이스 기준)
- 반응 시스템: 열·냉·습·압 4속성, 직교 전파, 다중 칸 이웃 중복 제거, 칸막이 차단, 열·냉 상쇄, 틱 카운터·상태 기계, 폭발 열의 다음 틱 반영(연쇄 지연), 용기(유리병 압력 누적·파열, 납 상자 시간 정지), 촉매, 풀무·부싯돌.
- 칸 상태: 그을림(열 +1) · 젖음(2턴 열 정지) · 구멍(배치 불가) · 산성 증기 누적으로 구멍.
- 전투: HP 30, 턴당 행동 2, 옮기기/회전/던지기/덮기·꺼내기/도구·붕대 사용/대기(피해 -2/행동), 상태 이상 6종(화상·동결·기름·중독·방어·화약 설치).
- 아이템 17종 + 조합 산출물 2종(소금 결정·불안정 결정) + 이벤트 촉매(잉걸).
- 적 6종 + 보스(누적 열, 화염 숨결 예고·취소, 페이즈 2 그을림). 층별 조우표, 정예 조우.
- 런: 층당 6노드(분기 2), 채집·이벤트 5종·캠프(수선·조합 6종·버리기), 3층 + 보스, 승리/패배 화면과 가설 판정용 통계(옮기기 비율, 예측 사용, 가방 점유율 등).
- 가독성: 남은 틱 상시 표시, 놓기 전 고스트 미리보기, 턴 종료 예측 오버레이(같은 솔버 함수 사용), 환경 게이지, 반경 하이라이트, 반응 로그, 4색 고정.
- 도감: 처음 목격한 상태 전이와 레시피를 `localStorage`에 기록·열람.
- 반응형: 900px 이하 1열, 600px 이하 아이콘 중심 셀. 모든 조작은 클릭/탭. 단축키 R/E/Esc.

**검증**
- Chromium(Playwright) 헤드리스에서 솔버·전투 회귀 테스트 29건 통과: 열·냉 상쇄, 연쇄 지연, 칸막이, 용기 밀폐·압 누적·파열, 다중 칸 이웃 중복, 결정성, 그을림/젖음, 기름병 상태 기계 전체 순서.
- 랜덤 정책 자동 플레이 여러 런(맵·전투·드롭·채집·이벤트·캠프·조합·수선·게임오버·보스)에서 런타임 오류 0건. 390px 폭에서 가로 넘침 없음.
- Opus 보조 에이전트가 기획서 대비 코드 검토 수행(25개 지적). 아래를 반영:
  - 용기 안 붕대 무한 회복, 적 행동 대기 중 추가 행동, 예측 호버가 도감·localStorage 오염, 폭발 열이 다음 전투로 누수 → 수정.
  - 젖음이 그을림을 지우던 문제와 젖음이 1틱만 가던 문제 → 칸 상태를 `hole/sc/soak/acid`로 분리해 수정.
  - 화상 투척 이중 피해, 보스 물 피해 0, 화약 설치 전역 플래그, 대기 보너스가 적마다 적용, 적 처치 후 환경 미갱신, 보스 누적 열 1턴 지연 → 수정.
  - 유리병 내용물 시간 정지 → 수명은 흐르되 외부 노출만 차단. 파열 시 내용물 효과가 적 전체에 적용.
  - 자리 교환(행동 2) 추가, 터치 기기용 두 번 탭 배치와 "예측 보기" 토글 추가, 피해 팝업 별도 레이어, 호버 시 부분 갱신.
  - 아이템 유형 테두리가 속성 4색과 충돌 → 선 스타일로 변경. 노드 비율 55/10/15/15로 정정, 정예 보상 중복 제거, 미니 가방 다중 칸 중복 표시 제거.
  - 불안정 결정의 "열 1·냉 1 동시 발산"은 상쇄 규칙과 충돌해 열 1만 발산하도록 변경(기획 재검토 필요).

**기획 대비 단순화·해석 (다음 버전에서 재검토)**
- 가방 안 폭발의 플레이어 피해는 자리표시자(기름 3, 화약 5, 유리병 파열 2, 불안정 결정 1).
- 유리병 파열은 플레이어 2 피해 + 내용물 효과 광역으로 처리(파편 4는 던졌을 때 2로 축소).
- 유리병 안에서 증기는 1틱 뒤 사라지며 압 1을 남긴다. 압력 폭탄은 산성 증기(압 1 × 3틱)를 가두는 것이 정석.
- 고블린은 용기 안 아이템은 훔치지 않지만 빈 용기는 훔칠 수 있다.
- 부싯돌은 인접 아이템 1개를 지정해 즉시 열 2를 가한다(같은 턴 안에 전이 발생).
- 드래그 앤 드롭 대신 클릭 선택 → 클릭 배치. 모바일에서는 고스트 미리보기(호버) 대신 배치 후 남은 틱을 확인.
- 정예 보상은 용기·도구 5종 중 무작위 2중 1. 채집은 반응물 2개 또는 도구 1개.
- 시작 가방: 화약은 유리병 안에서 시작(환경 열 +1이면 밖에서 즉시 폭발하기 때문).

**알려진 문제 / 다음 할 일**
- 가방이 비면 교착이 생길 수 있어 "런 포기" 버튼을 추가. 빈 가방 문제(H3)는 밸런스 단계에서 드롭·채집 수치 조정 필요.
- 수치는 전부 자리표시자. 적 HP·공격력·폭발 피해·대기 보너스는 W6 밸런싱 대상.
- 사운드 없음. 애니메이션은 피해 팝업·흔들림 최소치.
